# Franciska Torok's personal portfolio

## Installation

`npm install`
`npm run build`

Node v18.10.0

## Run Project

`cd portfolio`
`npm run dev`

Update **npm** before running:
`npm install -g npm@latest`
